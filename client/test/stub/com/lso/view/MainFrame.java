package com.lso.view;

import java.awt.Component;

// Takes the place of the real MainFrame in the tests (see ../../../../Rig.java):
// the client runs without a window, so the tests work without a screen.
// It has only what MainController uses.
public class MainFrame
{
    public void setGlassPane(Component component)
    {
    }

    public void add(Component component)
    {
    }
}
